// プロトタイプ宣言
// 引数としてint型の変数を2個受けとり、int型の値を返す関数sumを定義
int sum(int, int);
// 引数としてint型の変数を2個受けとり、int型の値を返す関数subを定義
int sub(int, int);
// 引数としてint型の変数を2個受けとり、int型の値を返す関数mulを定義
int mul(int, int);
// 引数としてint型の変数を2個受けとり、int型の値を返す関数divを定義
int div(int, int);
