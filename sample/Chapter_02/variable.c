#include <stdio.h>

int main(int argc, char *argv[])
{
    int number; // 変数の宣言

    number = 12345; // 変数numberに12345を代入

    printf("number is %d\n", number); // 変数numberが保持している
                                      // 値を画面に出力

    return 0;
}
