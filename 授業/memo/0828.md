* IAMユーザ、IAMグループ、IAM
    - アカウント権限
* IAMポリシー
    - サービスへの許可・拒否リスト

* ユーザ認証
    - メールアドレス＋パスワード
        * セキュリティが弱い
    - \+ MFA（多要素認証）を使用することが推奨 (ex)バイオメトリクス認証

1. VPCを作成
1. インターネットゲートウェイを作成（VPCにアタッチするのを忘れずに）
1. サブネットを作成
1. ルートテーブルを作成

---

* EC2サーバ(Windows- OSリストの一番上)を作成
    - セキュリティグループ作成
        * HTTP
        * RDP（マイIP）
* EC2サーバ(Linux)を作成
    - セキュリティグループ作成
        * HTTP
        * SSH（マイIP）
